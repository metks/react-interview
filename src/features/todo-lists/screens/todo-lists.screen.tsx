import { ReactNode } from "react";
import Layout from "../../../core/components/layout/Layout";

const TodoListsScreen = (): ReactNode => {
  return (
    <Layout>
      <h1>Todo Lists</h1>
      {/* Add your todo lists UI here */}
    </Layout>
  );
};

export default TodoListsScreen;
